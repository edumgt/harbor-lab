# Chapter 15 — Fractos RCS UI 배포 런북 (모델/NFS/TP=3/NodePort/HealthCheck)

## 목표
Fractos RCS(UI 폼)에서 GPU 컨테이너 인스턴스를 만들 때,
“실무에서 반드시 확인해야 하는 순서/체크포인트”를 런북으로 제공합니다.

## 실행
```bash
./run.sh
```

## 출력
- `artifacts/fractos_rcs_runbook.md` 생성
