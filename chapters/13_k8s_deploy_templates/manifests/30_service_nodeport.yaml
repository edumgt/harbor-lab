apiVersion: v1
kind: Service
metadata:
  name: vllm-nodeport
  namespace: llm-infer
spec:
  type: NodePort
  selector:
    app: vllm-server
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      nodePort: 30080
