# Chapter 01 — 환경 점검 (Windows + WSL2 + Docker Desktop)

## 목표
- WSL에서 `docker`, `docker compose`가 정상 동작하는지 확인
- Docker Desktop WSL Integration이 켜져 있는지 확인 포인트 안내

## 실행
```bash
./run.sh
```

## 체크포인트
- `docker version` 출력 OK
- `docker info`에서 Server가 Docker Desktop 엔진으로 연결되는지 확인
